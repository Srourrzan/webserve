server 
{
  listen 127.0.0.1:8080;
	server_name localhost;
	root "www";
	index "/index.html";
	location /cgi_bin
	{
		cgi on;
    cgi_ext py;
    root "www/cgi_bin";
    methods GET POST;
    client_max_body_size 10M;
	}
  location / {
    methods GET;
  }
}